<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.onestopcancercenter.onestopcancercenter">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".add_awareness_for_hospital"
            android:theme="@style/cancer1">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Admin_Sign_in" />
        <activity android:name=".Edit_hospital_info" />
        <activity android:name=".List_of_Hospitals" />
        <activity android:name=".Hospital_menu" />
        <activity android:name=".admin_HOSPITAL_Edit_MENU" />
        <activity android:name=".user_sign_up_page" />

        <activity android:name=".Menu_user" />
        <activity android:name=".Awareness_List" />
        <activity android:name=".Hospital_info" />
        <activity android:name=".Hospital_sign_up_page" />
        <activity android:name=".MainActivity" />
        <activity android:name=".user_sign_in_page" />
        <activity android:name=".Homepage"></activity>
        <activity android:name=".add_cancer"></activity>
        <activity android:name=".StoryAdd"></activity>
    </application>

</manifest>